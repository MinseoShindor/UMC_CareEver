<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.umc_careever">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mainlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mainlogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UMC_CareEver">
        <activity
            android:name=".signUp_profile"
            android:exported="true" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".UserCheckActivity" />
    </application>

</manifest>